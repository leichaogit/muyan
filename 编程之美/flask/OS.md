```
获取路径名：os.path.dirname()
获取文件所在目录的完整路径：os.path.dirname(__file__)
os.path.abspath(__file__)返回的是.py文件的绝对路径
BOOTSTRAP_SERVE_LOCAL = True
BOOTSTRAP_SERVE_LOCAL = True
在本地加载
```

```
Flask_Login:管理已登陆用户的用户会话
Werkzeug:计算密码的散列值并进行核对
itsdangerous:生成并核对加密安全令牌
```

